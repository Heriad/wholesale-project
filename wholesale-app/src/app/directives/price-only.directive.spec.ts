import { PriceOnlyDirective } from './price-only.directive';

describe('PriceOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
