import { SentenseCaseTypeDirective } from './sentense-case-type.directive';

describe('SentenseCaseTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new SentenseCaseTypeDirective();
    expect(directive).toBeTruthy();
  });
});
