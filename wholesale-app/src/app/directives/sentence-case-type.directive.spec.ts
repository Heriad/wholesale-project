import { SentenceCaseTypeDirective } from './sentence-case-type.directive';

describe('SentenceCaseTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new SentenceCaseTypeDirective();
    expect(directive).toBeTruthy();
  });
});
