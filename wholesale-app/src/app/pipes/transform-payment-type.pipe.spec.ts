import { TransformPaymentTypePipe } from './transform-payment-type.pipe';

describe('TransformPaymentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformPaymentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
