import { TransformOrderStatusPipe } from './transform-order-status.pipe';

describe('TransformOrderStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformOrderStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
